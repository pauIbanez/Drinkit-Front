const radius = 5;
const pageHorizontalPadding = 27;
const headerHeight = 130;

export const globalRadius = `${radius}px`;
export const globalPageHorizontalPadding = `${pageHorizontalPadding}px`;
export const globalHeaderHeight = `${headerHeight}px`;
